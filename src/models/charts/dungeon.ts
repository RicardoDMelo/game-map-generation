import { FiniteChart } from "./finite-chart";
import { Dimensions } from "../dimensions";

export class Dungeon extends FiniteChart {
    constructor(dimensions: Dimensions) {
        super(dimensions);
    }
}
